> 后续修复状态：同日多次借还唯一事件、修后真实价值、损坏存档校验、继承库存未知鉴定保护、买家实例锁定、双方议价上下文计数均已补回归。原缺陷过程保留如下，最新结果以 ../../artifacts/qa/final-system-verification.json 为准。

# 连续经营引擎独立复核

日期：2026-09-21；引擎正在同步改造。下面是本次读取并运行fixture时的发现，修复状态以最后验证为准。审查者未改 `src/engine.ts` 或 `src/main.ts`。

## 已验证的结构改进

- `Game.offer()`通过独立议价模块，接受当前公开估值/物品事实/种子；搜索未发现运行分支读取录像`price`或`quotes[].accepted`。
- `loadVisit()`已无sourceCash回填；旧选项与checkpointLog保留为只读兼容字段。
- 结算要求对应的当前物品实例，钱通过带幂等ID的现金流水转移；交易结束后不能重复结算。
- 玩家持有卡与客观物品事实分开。私有卡与公开卡都有记录，出售时可以审查当初的鉴定。

## 独立fixture发现并已发送维护者

| 严重性 | 复现 | 实际结果 | 应满足的不变量 |
| --- | --- | --- | --- |
| P1 | 资金充足，同一天连续清偿Darcy贷款并再次借款。 | 第三个清偿失败，余额14883仍不能偿还；现金事件ID只含贷款方与startedDay，碰撞。 | 每个被允许签发的贷款实例有独立清偿身份；不能把合法新贷款误判为旧事件重放。 |
| P1 | 对首尔邮票实例执行维修，检查本次实例真实价值。 | 展示值升至194，`trueValue`仍为维修前93。 | 真实属性/估值随维修后的实例改变；顾客与抵押估值不能继续使用修前模板。 |
| P2 | 将snapshot中的`profile.expertness`设为NaN、活动阶段negotiation设null，传给restore。 | 返回`ok:true`。 | 损坏存档应在进入运行态前被拒绝，保留原有效存档，不产生NaN价格或软锁。 |

fixture仅在Node里构造测试状态，未修改浏览器存档。临时复现脚本：`/tmp/nua-engine-independent-audit.ts`。

## 接下来通过真实UI验证的内容

`scripts/verify-free-play.mjs`使用三个隔离浏览器上下文，只通过点击、键盘和拖动UI操作；状态观察只读，不使用录像成交价表、不设置游戏对象、不写localStorage。

1. 拒绝第一件物品，再换顾客，确认现金及库存保留。
2. 按当前估值选不同收购价，确认实际扣款/单实例所有权；重复提交无第二次交易；刷新并Load恢复，再换人仍保留。
3. 首日所有交易都拒绝，确认可达动态日结及次日，没有虚构买卖；重复夜间输入不能重复扣息。

## 修复后回归结果

维护者修复后三项fixture独立重跑：同一天连续三轮借还均成功，现金事件ID不再重复；邮票实例真实价值从93升至187，展示含店铺效果为194；非有限声誉存档返回`ok:false`。这些结果针对修复后的调用路径，原始物品模板仍保持不变。

在冻结的5174构建上，真实浏览器三项场景全部通过，零浏览器错误：

- 拒绝首笔后换顾客：258仍为258。
- 根据显示估值支付106：258变152；重复输入不再次结算；刷新并Load仍为152，同一物品实例保留；换顾客后继续保留。
- 首日四笔交易全部拒绝：日结显示0买0卖，258余额；睡觉进入DAY7后233，只扣25一次。

完整报告：`artifacts/qa/free-play/report.json`。这三项是明确有限的自由选择回归，不是整局完成证明。

视觉检查同时发现成交气泡文字裁切，以及日结旧图片/文字边缘残留；已反馈维护者，未把“功能测试通过”当作视觉验收通过。

## 扩展回归：七场景

冻结构建 `/assets/index-BeHv9mgt.js` 再跑，七项全部通过、零浏览器错误。报告每场都记录同一实际bundle，避免混合不同构建。

新增结果：

- 未完成议价存档：首报59、顾客反报82；刷新后counter/history/rngState保留；接受后现金176。
- 全拒绝交易主线：访问53个不同来访，DAY6到DAY11，进入完成页并返回菜单。DAY10现金158不足必需罚款，通过真实Executor借款按钮取得1500，再付250；没有注入资金；最终现金1358。
- Challenge：30个遭遇全部可处理，完成后返回菜单。
- 日历分支：先全拒绝形成Day7余额233；从日历重载Day6，改为106买入首件后形成新Day7余额127；两个Day7分支都保留，重新加载旧分支确实恢复233。
- 日结新截图目视已无前一版旧物品条带和纸张旧字残留。

仍然不能把这组测试解释为所有交易策略、所有NPC人格、所有原作剧情、所有原作数值或完整视觉对照已通过。完整买卖的质量校验还必须结合其他独立测试。

## 原生剧情 Hook 接入后的候选版回归

2026-09-21 再次在冻结的 `/assets/index-DqrzjNyQ.js` 上跑完七场，7/7 通过、0 阻塞、0 浏览器错误；每个浏览器上下文都实际读取并断言这个 bundle 名。当前 `artifacts/qa/free-play/report.json` 对应这一轮，以上 BeHv9mgt 数字属于历史记录。

测试现在根据当前 UI 的 `phase/counter/result` 反馈，在最多六次实际报价内等待还价或交易终止。未硬写“前两次拒绝”，没有查询录像成交价。首客未完成品牌鉴定时可拒绝反复低价并离店，因此“待还价存档”不再假设未举证时必有 counter：

- 实际打开材质工具并检查背包，自动进入材料书页，拖入 Canvas。
- 从品牌书页拖入 Wrong Material，公开估值由118降为55；此时现金和库存仍不变。
- 按当前55估值的一半取整报价27，得到真实 counter38；刷新、Load 后保存的 counter/history/rngState 一致；接受当前还价后现金258→220。
- 另外六条路径仍全部成立，包括从DAY6到DAY11全拒绝并完成、30次挑战并退出、旧/新日历分支并存。

补充真实 UI 检查见 `native-refused-card.json/png`：同一个 DqrzjNyQ 构建下，拖入 Wrong or Missing Slogan 被首客明确拒绝，原标签、118估值、258现金及库存不变，英文气泡可见。该检查没有直接改状态或存档。

人工检查了 `alternate-price-save-entered-offer.png` 与 `first-price-response.png`：报价器原生数字106清楚可见，提交后的英文拒绝反馈可见。透明 `quote-input` 是 Canvas 报价器的键盘输入层；它的值与绘制用 quote 一致，本次未发现输入不可见导致无法报价的半成品状态。

本轮还向维护者提交一个未被七路径覆盖的只读边界：`clearConversation` 应核实是否同步清除独立议价对象的 `customerOffers`，因为该字段参与首轮判断及部分人格的总轮次；不能仅依据这七条通过路径排除这个问题。
